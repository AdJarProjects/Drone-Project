"""
tools/gen_mode_config.py  --  Capture your INAV AUX/mode mappings.

You do NOT need to write this one. uNAVlib already ships a gen_mode_config
utility that connects to your board and generates a JSON file with board
info and your AUX-mode configuration, so you can reference your real modes
in code instead of hardcoding channel numbers.

Use the library's version. From the uNAVlib examples/tools, run its
gen_mode_config against your FC, then commit the resulting JSON here and
load it where ArmGate / Control need your AUX channel layout.

This stub exists only as a placeholder/reminder in the repo.
"""

# Placeholder. Use uNAVlib's bundled gen_mode_config instead.
